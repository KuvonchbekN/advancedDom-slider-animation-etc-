const randomInt = (min, max) => Math.floor(Math.random() * (max - min + 1) + min); //INPUT BETWEEN MIN AND MAX



//Creating random Color
const randomColor = () => {
  return `rgb(${randomInt(0,255)},${randomInt(0,255)},${randomInt(0,255)})`
};
console.log(randomColor());
